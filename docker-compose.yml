services:
  api:
    build: ./backend
    environment:
      - PORT=3000
    volumes:
      - ./backend/db:/app/db
    restart: unless-stopped

  web:
    build: ./frontend
    depends_on:
      - api
    ports:
      - "80:80"
    restart: unless-stopped
